\usepackage{longtable}
\usepackage{pdflscape}
\usepackage{placeins}
\usepackage{graphicx}
\usepackage{float}   %<-can't use with floatrow package (was used below)
%--now trying the floatrow package (need to comment out approach above based on float package)
%--\usepackage{floatrow}  %--resets a bunch of stuff
\usepackage{setspace}

%--turn on page numbering
\pagenumbering{arabic}
%--turn off page numbering
%--\usepackage{nopageno}

%--define caption characteristics for all float types (must override for individual float types)
\usepackage[tableposition=t,figureposition=b]{caption}
\setlength{\LTcapwidth}{1.0\linewidth}       %--for use with longtable
%--set up table floats
\captionsetup[table]{position        = above,
                     justification   = raggedright, 
                     labelsep        = period,
                     singlelinecheck = false}
% \floatsetup[table]{capposition=top,
%                    captionskip=0pt}
%--set up figure floats
\captionsetup[figure]{position       = below,
                     justification   = raggedright, 
                     labelsep        = period,
                     singlelinecheck = false}
% \floatsetup[figure]{capposition=bottom,
%                     captionskip=0pt}


